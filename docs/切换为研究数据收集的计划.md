# CREST 论文数据准备阶段切换计划

## Summary
- 本阶段从算法优化切换为论文数据准备。
- 当前算法固定为 `crest`。
- 文档入口后续迁移到 `ResearchDoc/`，避免继续受旧 `docs/` 探索文档影响。
- 本轮修改范围保持很小：只更新 `AGENTS.md` 并新增 `ResearchDoc/` 阶段文档。
- 不创建新分支、不删除旧文件、不修改算法和旧工具。

## Key Changes
- 在当前分支直接修改，不新建 git branch。
- 更新 `AGENTS.md`：
  - 默认论文数据准备文档目录改为 `ResearchDoc/`。
  - 明确当前阶段不要读取或搜索 `docs/` 目录，除非用户显式要求。
  - 当前研究对象从 EvidenceRank 迭代优化改为 CREST 论文数据准备。
  - 保留旧经验沉淀，但不让旧 VibeResearch / EvidRank 流程主导新阶段。
- 新增 `ResearchDoc/README.md`：
  - 作为论文数据准备阶段总入口。
  - 记录阶段目标、文档规则、可复现要求。
- 新增 `ResearchDoc/CREST_paper_data_plan.md`：
  - 记录 CREST 论文数据准备目标、实验命令、指标表、artifact 路径和后续任务清单。

## Test Plan
- `git diff --check`
- `rg -n "docs/" AGENTS.md ResearchDoc`
- `git status --short --branch`

## Assumptions
- 当前分支工作区干净，可以直接进行小范围文档契约修改。
- 本轮不删除 `docs/`、`VibeResearchTools/`、历史输出或旧探索文件。
- 旧工具 `VibeResearchTools/evidrank_lab.py` 暂不修改。
- 后续如果开始新增论文数据脚本、批量生成表格或清理旧文件，再考虑新建分支。
